# Kytos secret-scan allowlist. One term or regex per line; `#` comments allowed.
#
# A secret match is suppressed if it CONTAINS any term listed here. Use this
# ONLY for deliberate stubs/examples that would otherwise be high-signal, never
# to whitelist a real credential.
#
# Project-owned placeholders:
kytos@example.com
AKIA0000000000000000
sk_test_
# Generated fixture gene hashes / run ids (deterministic, not secrets):
gene_order_sha